# Bun configuration
# https://bun.sh/docs/runtime/bunfig

[install]
# Prefer exact versions from lockfile
exact = false

[run]
# Use .env files automatically
env-file = [".env", ".env.local"]

[test]
coverage = false
coverage-dir = "coverage"
